const divide= (a, b)=>{
    let result = a/b;
    return result;
}
console.log (divide(2000, 100));

const square= num=> num * num;
const add= (a, b)=>{
    let result= a + b;
    return result;
}
console.log(add(3,4));